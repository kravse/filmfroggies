{
  "name": "moviecollector",
  "version": "1.0.0",
  "private": true,
  "description": "Search TMDB and collect movies into ordered lists. Static site; lists live in the browser.",
  "scripts": {
    "serve": "node server.js",
    "bundle": "node scripts/bundle-app-js.js",
    "sync-worker-lib": "node scripts/sync-worker-lib.js",
    "bundle:letterboxd": "node scripts/bundle-letterboxd-tool.js",
    "letterboxd-import": "node scripts/letterboxd-import.js",
    "test": "node --test \"test/*.test.js\"",
    "build": "node build.js"
  },
  "devDependencies": {
    "express": "^4.21.2"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
