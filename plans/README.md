# Plans

Design and migration notes for CineQueue. **Not part of the deployed site.**

- Production (Netlify) publishes only `build/` — nothing here is copied.
- Local dev servers block `/plans` requests (see `scripts/lib/static-guard.js`).
